# PySide6-Essentials (not the full PySide6 meta-package) -- EtherWave only
# uses QtCore/QtGui/QtWidgets/QtNetwork, all in Essentials. The full
# "PySide6" package also pulls in Addons (WebEngine, Qt3D, Charts,
# DataVisualization, ...), which are unused here but add ~100+ MB to the
# PyInstaller macOS bundle for nothing.
PySide6-Essentials>=6.6
sounddevice>=0.4.6
numpy>=1.24

# macOS-only: lets client/gui.py toggle the Dock icon's visibility
# (NSApplication.setActivationPolicy_) so it only shows while the window is
# actually open, not while minimized to the tray/menu bar. PySide6 has no
# equivalent of its own for this AppKit-level API. Harmless/unused on Linux
# (the server has no Dock), and client/gui.py degrades gracefully if it's
# somehow missing rather than crashing.
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"
